package com.kms.grupo02

rule "Nada"
	when
		$j : Jugada (picas==0, fijas==0)
	then
        System.out.println("Regla para " + $j.getPicas() + " y " + $j.getFijas() + " es decir nada ");  
        $j.descartar($j.getDigitoUno(), $j.getDigitoDos());   
end

